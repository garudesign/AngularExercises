
module.exports = function(gulp, plugins, config, functions) {
	/*var builder = new plugins.systemjsBuilder('', 'src/js/utilities/systemjs.config.js');

	gulp.task('bundle', function() {
		builder.config({
			paths: {
				'node_modules/': '/node_modules' // or whatever this path is
			}
		});
		builder
			.bundle('src/js/init.js', 'src/js/new.js', { defaultExtension: 'js' })
			.then(function() {
				console.log('Build complete');
			})
			.catch(function(err) {
				console.log('Build error');
				console.log(err);
			});
	});*/
};
